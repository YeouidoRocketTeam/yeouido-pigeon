# Netlify 배포 설정 (yeouido-pigeon)
[build]
  command = "npm run build"
  publish = "dist"

# SPA 라우팅: 모든 경로를 index.html로 (React Router 대응)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
